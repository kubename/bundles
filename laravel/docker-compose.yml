version: "3.4"

services:
  php-fpm:
    build: docker/php-fpm
    volumes:
      - ./stubs:/code/public
    depends_on:
      - caddy

  caddy:
    build: docker/caddy
    volumes:
    - ./stubs:/code/public
    ports:
      - 8080:80
